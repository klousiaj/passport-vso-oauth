<% layout('boilerplate') %>
<% if (!user) { %>
	<h2>Welcome! Please log in.</h2>
<% } else { %>
	<h2>Hello, <%= user.displayName %>.</h2>
  <p> View your profile details on the <a href="/profile">profile</a> page.</p>
<% } %>